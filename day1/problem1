

#include <stdio.h>

int main() {
    int n, sum = 0;

    printf("Enter a positive integer: ");
    scanf("%d", &n);

    // Using formula: sum = n*(n+1)/2
    sum = n * (n + 1) / 2;

    printf("Sum of first %d natural numbers is: %d\n", n, sum);

    return 0;
}
