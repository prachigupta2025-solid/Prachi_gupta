#include <stdio.h>

int main() {
    int a, b;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    while (b != 0) {
        int temp = b;
        b = a % b;  // remainder
        a = temp;   // update a
    }

    printf("GCD = %d\n", a);

    return 0;
}
